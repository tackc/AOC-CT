const fs = require("fs");
let input = fs.readFile("./input.txt", "utf8").split("\n");

console.log(input);

// iterate through input
input.forEach((element) => {});
// split data from input into usable object key value pairs

// pass out compiled object

const dataObject = {};

function passwordValidator(arr) {
  console.log(arr);
}

module.exports = passwordValidator;
